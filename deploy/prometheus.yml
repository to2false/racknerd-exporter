global:
  scrape_interval: 60s
  scrape_timeout: 15s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: racknerd
    static_configs:
      - targets: [racknerd-exporter:9725]
